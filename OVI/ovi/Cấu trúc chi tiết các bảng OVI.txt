Cấu trúc chi tiết các bảng
🏷 Bảng service kiennv
CREATE TABLE service (
    id bigint PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    short_description TEXT,
    description TEXT,
    imageData TEXT,
    status int,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

📰 Bảng news kiennv
CREATE TABLE news (
    id bigint PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    summary TEXT,
    content TEXT,
    image_data VARCHAR(500),
    author VARCHAR(100),
    status int,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

📞 Bảng contact_message Kiennv
CREATE TABLE contact_message (
    id bigint PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(150) NOT NULL,
    phone VARCHAR(20),
    message TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    status int
);

👥 Bảng featured_person tuha
CREATE TABLE featured_person (
    id bigint PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    role VARCHAR(255),
    description TEXT,
    imageData TEXT,
    type VARCHAR(50) DEFAULT 'COMPANY',
    -- COMPANY = đối tác, CLIENT = khách hàng tiêu biểu, LEADER = Ban lãnh đạo
    status BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

🔐 (Tùy chọn) Bảng admin_user tuha
CREATE TABLE admin_user (
    id bigint PRIMARY KEY,
    username VARCHAR(100) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    full_name VARCHAR(255),
    email VARCHAR(255),
    role VARCHAR(50) DEFAULT 'ADMIN',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE company_profile (
    id bigint PRIMARY KEY,
    company_name VARCHAR(255),
    description TEXT,
    file_data OID NOT NULL,
    file_name VARCHAR(255) NOT NULL,
    file_type VARCHAR(100) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP
);